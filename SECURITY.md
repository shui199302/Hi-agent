# 安全策略

## 支持范围

Hi-agent 当前处于早期开发阶段。安全修复优先落到最新代码；正式版本发布后，本文件将补充明确的
版本支持矩阵。默认部署边界是单机回环地址，不应把开发模式直接视为公网生产配置。

## 报告漏洞

请通过项目维护者提供的私密渠道报告安全问题，不要先创建公开 Issue，也不要在报告中附带真实
密钥或用户数据。如果仓库托管平台支持私密安全公告，请优先使用该入口。

报告建议包含：

- 受影响的版本或提交；
- 前置条件和最小复现步骤；
- 实际影响及可能的攻击路径；
- 已知缓解方式；
- 必要时提供脱敏后的日志或样例文件。

维护者确认问题后，应尽快给出影响评估和修复计划；在修复可用前，请避免公开可直接利用的细节。

## 安全边界

### 身份与租户

- 开发模式的短信验证码和微信授权是本地模拟能力，不适合公网。
- 生产模式必须接入真实身份 Provider、启用 HTTPS，并设置
  `HI_AGENT_AUTH_COOKIE_SECURE=true`。
- 所有用户资源查询和变更都必须验证 `owner_id`；跨用户访问返回 404。
- Cookie、CSRF、验证码有效期、重发限制、尝试锁定和审计逻辑不得为调试而绕过。

### 凭据

- `.env` 不提交版本库；仅 `.env.example` 保存无敏感值的配置结构。
- 数据库只保存密钥环境变量名，不保存值。
- 密钥不得出现在日志、异常详情、SSE、API 响应、浏览器存储、截图或测试夹具中。
- 怀疑泄露后应立即撤销并轮换相关凭据，同时检查提交历史和日志副本。

### Agent 工具与联网

- 未知工具按写入风险处理；写入和执行操作必须人工审批。
- 远程 MCP 默认关闭，只允许 HTTPS，并同时满足全局和服务级许可。
- stdio MCP 使用固定命令及参数数组，不经 shell；环境变量按允许的变量名解析。
- 远程 Skill 必须固定版本、校验来源和哈希并通过安全扫描；发现或安装阶段不得执行其脚本。
- 模型、检索文档、网页、工具输出和 Skill 内容都可能包含提示注入，必须作为不可信数据隔离。

### 文件与数据

- 文件访问必须使用规范化路径并限制在授权根目录内，拒绝路径穿越和符号链接逃逸。
- 上传需要限制大小、类型和解析资源消耗；OCR、文档解析和制品生成必须有页数、时间及体积上限。
- 删除文档时同时清理元数据、上传文件和向量；不得把其他用户的内容加入回答或引用。
- `data/` 和 `logs/` 可能含敏感信息，不应提交、公开或作为普通诊断附件上传。

### 部署

- 默认仅监听 `127.0.0.1`/`localhost`/`::1`。
- 公网部署需要额外的 TLS、反向代理、真实认证 Provider、访问控制、备份、监控和日志脱敏评审。
- vLLM 模型权重、许可证、显存隔离及其 API Key 由部署者负责。
- 不可用的模型端点应显式失败，不得静默切换到第三方云服务。

## 安全变更检查清单

- 是否扩大了文件、网络、进程或数据库访问范围？
- 是否改变了审批、认证、CSRF、跨用户 404 或失败关闭语义？
- 是否可能把密钥或不可信内容带入日志、提示词或响应？
- 是否为越权、路径逃逸、提示注入、SSRF 和资源耗尽补充了回归测试？
